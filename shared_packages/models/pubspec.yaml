name: zeni_models
description: Shared data models for the Zeni ride-hailing platform
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  json_annotation: ^4.9.0
  freezed_annotation: ^2.4.4

dev_dependencies:
  build_runner: ^2.4.9
  json_serializable: ^6.8.0
  freezed: ^2.5.2
